<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gym_platform">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="득근득근"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".T_reviewActivity"></activity>
        <activity android:name=".HealthpayActivity" />
        <activity android:name=".HealthpayActivity1" />
        <activity android:name=".HealthpayActivity3" />
        <activity android:name=".HealthpayActivity6" />
        <activity android:name=".HealthpayActivity12" />
        <activity android:name=".TrainerActivity" />
        <activity android:name=".PayActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NavigationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".GymActivity" />
        <activity android:name=".ReservationListActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ReviewActivity" />
    </application>

</manifest>